{
  "name": "plenty-dev",
  "private": true,
  "scripts": {
    "local-services": "hc run-local-services --bootstrap-port $BOOTSTRAP_PORT --signal-port $SIGNAL_PORT",
    "test": "pnpm run build:happ && nix flake check -L && pnpm -F tests test",
    "happ:build-debug": "nix build -L .#plenty.meta.debug -o workdir/plenty.happ",
    "happ:build": "nix build -L .#plenty -o workdir/plenty.happ",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@holochain-playground/cli": "^0.1.0",
    "@tauri-apps/cli": "^2.0.0-alpha.17",
    "concurrently": "^6.2.1",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "pnpm": ">=9.0.0"
  }
}